<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

<% if current_user != @user %>
   <p> You cannot view other users information </p>
<% else %>

<h1>



All My Exercises
<br>
<%#= Exercise.all.where(title: "HSPU") %>
</h1>


ALL EXERCISES BY CREATION DATE:
<br><br>
<p> Exercise Name &nbsp;&nbsp;&nbsp;&nbsp; || &nbsp;&nbsp;&nbsp;&nbsp; Workout Description</p>
<% @user.workouts.order('date DESC').each do |w| %>
<!-- @user.workouts.find_by_id(7).exercises -->
<p>
		
   	Workout: <%= w.description %>	<%= link_to 'Details', user_workout_exercises_path(params[:user_id], w.id) %>
 			<br>

   	<% w.exercises.each do |f| %>
   	<%= f.title   %> <%= link_to 'Show Exercise', user_workout_exercise_path(params[:user_id], w.id, f.id) %>
   	<br>
  
   <% end %>
   

   </p> 

<% end %>


<% end %>

<%= link_to 'Back To All Workouts', user_workouts_path %>

<% console %>

</body>
</html>